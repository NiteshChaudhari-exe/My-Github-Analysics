# GitHub OAuth app settings
# Register an OAuth App at https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# Where the server should redirect after OAuth (optional)
# Default: http://localhost:4000/auth/github/callback
OAUTH_REDIRECT_URI=http://localhost:4000/auth/github/callback

# URL of the client app (used to redirect after successful auth)
CLIENT_APP_URL=http://localhost:3000

# If your client is hosted on a different origin than the default,
# set REACT_APP_API_SERVER in the client environment (e.g. .env for CRA)
# REACT_APP_API_SERVER=http://localhost:4000
